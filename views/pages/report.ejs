!DOCTYPE html>
<<html>

    <head>
        <% include ../partials/header.ejs %>
    </head>

    <body>

        <% include ../partials/nav.ejs %>

            <body>
                <div class="container">

                    <h1>Display Report</h1>
                    <p>
                        The Report id is:
                        <%= result[0].report_id + "Today's High was: " + result[0].report_weather_high + "degrees & " + result[0].report_weather_conditions %>.
                    </p>
                    <p>
                        Report Notes:
                        <%= result[0].report_content %>
                    </p>

                </div>
            </body>

            </html>